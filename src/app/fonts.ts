import { Inconsolata, Inter, Lora } from "@next/font/google";

export const fonts = {
  inconsolata: Inconsolata({
    subsets: ["latin"],
  }),
  inter: Inter({
    subsets: ["latin"],
  }),
  lora: Lora({
    subsets: ["latin"],
  }),
};
